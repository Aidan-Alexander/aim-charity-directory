# Secrets — never commit tokens
.env
.env.*
*.token
*.pem

# Raw Airtable dumps (contain unpublished fields) and scratch output
raw/
tmp/
*.log

# Dependencies and OS noise
node_modules/
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Design fixture images are regenerated by --ignore-ready builds; only the JSON is tracked
design/preview-data/img/
